<article class="event">
	<p class="date">
		<span class="day">{{ t.date|date:"j" }}</span>
		<span class="month">{{ t.date|date:"M" }}</span>
	</p>
	<h3>{{ t.title }}</h3>
	<p>Conference: <a href="{{ t.link }}">{{ t.name }}</a></p>
	<p>{{ t.location }}</p>
</article>
